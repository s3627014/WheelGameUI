package controller.interfaces;

import java.awt.event.ActionListener;

//Methods were removed on refactor but keeping interface so I only have to extend to wheelPanelController in controller.
public interface WheelPanelController extends ActionListener, PropertyChangeSupportSetter {

}
